import{NgForm,FormGroup,Validators,FormBuilder,FormControl} from '@angular/forms';

export class UserData {
    userName: string = '';
    password: string = '';
    userType:number=0;
    //fromLoginGroup:FormGroup;
   // constructor(){
     //   var _builder= new FormBuilder();
      //  this.fromLoginGroup=_builder.group({});
        ////this.fromLoginGroup.addControl("UserNameControl",new FormControl('',Validators.required))
        //this.fromLoginGroup.addControl("PasswordControl",new FormControl('',Validators.required))
  //  }
    
}